# ============ Maven ============
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
*.jar
*.war

# ============ IDE ============
.idea/
*.iml
.vscode/
.project
.classpath
.settings/

# ============ OS ============
.DS_Store
Thumbs.db
*.swp
*.swo

# ============ Env & Secrets ============
.env
*.log

# ============ Docker volumes ============
# (data is in named volumes, nothing to ignore here)

# ============ Maven Wrapper (keep the wrapper jar) ============
# !.mvn/wrapper/maven-wrapper.jar
